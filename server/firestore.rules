rules_version = '2';

// Firestore にはリフレッシュトークンなどの機密情報を保存するため、
// クライアント（アプリ）からの直接アクセスはすべて禁止する。
// バックエンド（Cloud Run）は Admin SDK 経由でアクセスするため、このルールの影響を受けない。
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
